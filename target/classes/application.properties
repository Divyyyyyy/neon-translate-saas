# ==========================================
# 1. DATABASE CONNECTION (RAILWAY READY)
# ==========================================
# Railway automatically provides these variables. 
# Local par chalane ke liye aap inko environment variables mein set kar sakte hain.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/neon_translate?createDatabaseIfNotExist=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ==========================================
# 2. EMAIL SETUP (NEURAL OTP ENGINE)
# ==========================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# Username aur Password variables se uthayega, 
# par agar variable nahi mila toh niche wala default use karega.
spring.mail.username=${MAIL_USERNAME:rajputdivyarajsingh@gmail.com}
spring.mail.password=${MAIL_PASSWORD:zark cgwu iipc bbcw}

# Critical Mail Properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Connection Timeouts (Connection Timed Out error fix karne ke liye)
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000